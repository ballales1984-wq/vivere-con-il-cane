# Build Command

This is a Django project. Run:

```
pip install -r requirements.txt
python manage.py migrate --no-input
python manage.py collectstatic --noinput
```

Run the app with:
```
python start.py
```